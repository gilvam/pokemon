/** Rarity band derived from the species `capture_rate` (0–255, higher = easier to catch). */
export enum RarityTier {
  COMMON = 'common',
  UNCOMMON = 'uncommon',
  RARE = 'rare',
  VERY_RARE = 'very-rare',
}
