/** A type's accessible color pair: a soft background tint and the text drawn on it. */
export class TypeColor {
  constructor(
    public background = '#e0e0e0',
    public text = '#1f2937',
  ) {}
}
