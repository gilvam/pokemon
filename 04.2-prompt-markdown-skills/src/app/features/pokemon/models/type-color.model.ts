/** A type's accessible color pair: a soft pastel background tint and the text drawn on it. */
export class TypeColor {
  constructor(
    public background = '#e5e7eb',
    public text = '#1f2937',
  ) {}
}
